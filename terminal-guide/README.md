# Terminal Guide

A single-page HTML cheat sheet covering the shell, git, SSH, and tmux workflows
Noah uses day-to-day. Open `noahs_terminal_guide.html` in any browser.

No install — just a static file. Drop it on your desktop or bookmark the local
path.
